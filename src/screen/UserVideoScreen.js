import React from 'react';
import { View, Text } from 'react-native';

const UserVideoScreen = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>User Video Screen</Text>
        </View>
    );
};

export default UserVideoScreen;
